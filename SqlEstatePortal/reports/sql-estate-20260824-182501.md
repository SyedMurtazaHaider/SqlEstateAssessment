# SQL Estate Assessment

Generated: 08/24/2026 18:25:01

## Executive summary

- Servers assessed: 2 (reachable 2, unreachable 0)
- End of support: 0
- Allocated data+log: 105.11 GB
- Estimated licensed cores: 16
- Findings: Critical 20, High 12, Medium 25, Low 5

## Findings

- **Critical** / SLA / MURTAZA\SQL2022: Database 'DocManagementDb' last full backup is 268 hours old (SLA 24h). -- Fix the backup job and verify backups are restorable off-box.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'HealthCare_2' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'HealthCare_3' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'varianenm' last full backup is 9338 hours old (SLA 24h). -- Fix the backup job and verify backups are restorable off-box.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'HealthCare_4' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'HealthCare3' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'HealthCare1' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'CountyMedical' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'HealthCare' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'HealthCare_6' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'master' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'model' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'msdb' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'SqlEstatePortal' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQL2022: Database 'AuntieAI' has no full backup recorded in msdb. -- Take a full backup immediately and enroll the database in the estate backup SLA.
- **Critical** / SLA / MURTAZA\SQLEXPRESS: Database 'FISOHCM' last full backup is 14255 hours old (SLA 24h). -- Fix the backup job and verify backups are restorable off-box.
- **Critical** / SLA / MURTAZA\SQLEXPRESS: Database 'model' last full backup is 16876 hours old (SLA 24h). -- Fix the backup job and verify backups are restorable off-box.
- **Critical** / SLA / MURTAZA\SQLEXPRESS: Database 'EliteCarsInternational' last full backup is 11131 hours old (SLA 24h). -- Fix the backup job and verify backups are restorable off-box.
- **Critical** / SLA / MURTAZA\SQLEXPRESS: Database 'msdb' last full backup is 16876 hours old (SLA 24h). -- Fix the backup job and verify backups are restorable off-box.
- **Critical** / SLA / MURTAZA\SQLEXPRESS: Database 'master' last full backup is 16876 hours old (SLA 24h). -- Fix the backup job and verify backups are restorable off-box.
- **High** / Licensing / MURTAZA\SQL2022: Developer edition detected. -- Developer edition is not licensed for production workloads.
- **High** / SLA / MURTAZA\SQL2022: Database 'HealthCare_3' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **High** / SLA / MURTAZA\SQL2022: Database 'HealthCare_6' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **High** / SLA / MURTAZA\SQL2022: Database 'HealthCare_4' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **High** / SLA / MURTAZA\SQL2022: Database 'HealthCare_2' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **High** / SLA / MURTAZA\SQL2022: Database 'SqlEstatePortal' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **High** / SLA / MURTAZA\SQL2022: Database 'CountyMedical' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **High** / SLA / MURTAZA\SQL2022: Database 'AuntieAI' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **High** / SLA / MURTAZA\SQL2022: Database 'HealthCare' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **High** / SLA / MURTAZA\SQL2022: Database 'DocManagementDb' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **High** / SLA / MURTAZA\SQL2022: Database 'HealthCare3' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **High** / SLA / MURTAZA\SQL2022: Database 'HealthCare1' is FULL but has no log backup. -- Start log backups or switch to SIMPLE if point-in-time recovery is not required.
- **Medium** / Performance / MURTAZA\SQL2022: max server memory is 2147483647 MB vs 32644 MB physical. -- Cap max server memory to leave RAM for the OS (typically 4 GB+ on dedicated hosts, more with other services).
- **Medium** / SLA / MURTAZA\SQL2022: Database 'SqlEstatePortal' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'AuntieAI' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'master' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'varianenm' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'HealthCare1' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'model' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'msdb' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'HealthCare_6' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'HealthCare' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'HealthCare3' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'CountyMedical' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'DocManagementDb' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'HealthCare_3' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'HealthCare_2' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQL2022: Database 'HealthCare_4' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / Licensing / MURTAZA\SQLEXPRESS: Express edition detected (resource limits apply). -- Confirm Express is appropriate; it is not sized for most production OLTP estates.
- **Medium** / Performance / MURTAZA\SQLEXPRESS: max server memory is 2147483647 MB vs 32644 MB physical. -- Cap max server memory to leave RAM for the OS (typically 4 GB+ on dedicated hosts, more with other services).
- **Medium** / SLA / MURTAZA\SQLEXPRESS: Database 'msdb' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQLEXPRESS: Database 'EliteCarsInternational' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQLEXPRESS: Database 'FISOHCM' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQLEXPRESS: Database 'master' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / SLA / MURTAZA\SQLEXPRESS: Database 'model' has no LastGoodCheckDbTime. -- Run DBCC CHECKDB and capture last-known-good; property is unavailable on some older builds.
- **Medium** / Standards / MURTAZA\SQLEXPRESS: Database 'FISOHCM' has AUTO_CLOSE on. -- Disable AUTO_CLOSE on server databases; it causes reopen latency.
- **Medium** / Standards / MURTAZA\SQLEXPRESS: Database 'EliteCarsInternational' has AUTO_CLOSE on. -- Disable AUTO_CLOSE on server databases; it causes reopen latency.
- **Low** / Cost / MURTAZA\SQL2022: Backup compression is not the instance default. -- Enable backup compression default to reduce backup storage and duration (test CPU impact).
- **Low** / Security / MURTAZA\SQL2022: Engine service account is NT Service\MSSQL$SQL2022. -- Use a Group Managed Service Account (gMSA) for production instances.
- **Low** / Standards / MURTAZA\SQL2022: optimize for ad hoc workloads is OFF. -- Enable this on most OLTP systems to reduce plan-cache bloat from one-shot queries.
- **Low** / Security / MURTAZA\SQLEXPRESS: Engine service account is NT Service\MSSQL$SQLEXPRESS. -- Use a Group Managed Service Account (gMSA) for production instances.
- **Low** / Standards / MURTAZA\SQLEXPRESS: optimize for ad hoc workloads is OFF. -- Enable this on most OLTP systems to reduce plan-cache bloat from one-shot queries.
- **Info** / Security / MURTAZA\SQL2022: Mixed authentication is enabled (1 enabled SQL logins). -- Prefer Windows authentication only where possible.
- **Info** / Security / MURTAZA\SQLEXPRESS: Mixed authentication is enabled (1 enabled SQL logins). -- Prefer Windows authentication only where possible.


